[project]
name = "docling-pipelines"
dynamic = ["version"]
description = "Docling Pipelines — a composable document processing pipeline framework built on Docling."
readme = "README.md"
requires-python = ">=3.12,<3.13"
maintainers = [
    { name = "Jojo Joseph", email = "jo-in@ibm.com" },
    { name = "Aditya Rohan Singh", email = "adityars@ibm.com" },
    { name = "Nipun Bayas", email = "nsbayas@ibm.com" },
    { name = "Paul Baby", email = "paulbaby@ibm.com" },
    { name = "Docling Pipeline Team", email = "docling_pipeline_team-dg@ibm.com"},
]

dependencies = [
    "accelerate==1.13.0",
    "aiohttp==3.14.3",
    "alembic==1.18.4",
    "azure-storage-blob==12.28.0",
    "babel==2.18.0",
    "boto3==1.42.55",
    "botocore==1.42.55",
    "box-sdk-gen==1.17.0",
    "confluent-kafka==2.15.1",
    "cryptography==50.0.0",
    "data-prep-toolkit-transforms==1.1.8",
    "datatrove==0.10.0",
    "datefinder==1.0.0",
    "docling[rapidocr]==2.127.0",
    "dropbox==12.2.1",
    "duckdb==1.3.2",
    "fastapi==0.141.1",
    "fasttext-wheel==0.9.2",
    "filelock==3.24.2",
    "ftfy==6.3.1",
    "google-api-python-client==2.190.0",
    "google-auth-httplib2==0.3.0",
    "google-auth-oauthlib==1.2.4",
    "google-cloud-storage==3.9.0",
    "google-re2==1.1.20251105",
    "griffe==1.15.0",
    "httpx==0.28.1",
    "ibm-cos-sdk==2.14.3",
    "ibm-watsonx-ai==1.5.10",
    "langchain==1.3.9",
    "langchain-classic==1.0.7",
    "langchain-community==0.4.1",
    "langchain-core==1.4.6",
    "langchain-experimental==0.4.1",
    "langchain-text-splitters==1.1.2",
    "langdetect==1.0.9",
    "langsmith==0.8.18",
    "litellm==1.84.0",
    "lxml==6.1.0",
    "mako==1.3.12",  # CVE-2026-44307: Security fix for Mako vulnerability
    "mlx==0.32.2; sys_platform == 'darwin'",
    "msal==1.37.0",
    "nltk==3.10.3",
    "numpy>=2.0.0,<3.0.0",
    "o365==2.1.9",
    "ollama==0.6.1",
    "openai==2.33.0",
    "opensearch-py==3.1.0",
    "orjson==3.11.7",
    "pandas==2.3.3",
    "passlib[bcrypt]==1.7.4",
    "pillow==12.3.0",
    "polars==1.32.3",
    "prefect==3.8.3",
    "psycopg2-binary==2.9.12",
    "pyarrow==24.0.0",
    "pyasn1==0.6.4",
    "pydantic==2.12.5",
    "pydantic-settings==2.14.2",
    "pygments==2.20.0",
    "pyiceberg-core==0.7.0",
    "pyiceberg[glue]==0.9.1",
    "pyjwt==2.13.0",
    "milvus-lite==3.2.1",
    "pymilvus==2.6.13",
    "pyopenssl==26.4.0",
    "pypdf==6.16.1",
    "pyphen==0.17.2",
    "python-dotenv==1.2.2",
    "python-jose[cryptography]==3.5.0",
    "python-ldap==3.4.5",
    "pyyaml==6.0.3",
    "requests==2.33.0",
    "sentence-transformers==5.4.1",
    "setuptools==83.0.0",
    "spacy>=3.7.4",
    "sqlalchemy==2.0.49",
    "sqlglot==27.13.2",
    "sqlmodel==0.0.38",
    "tabulate==0.9.0",
    "tenacity==8.3.0",
    "toml==0.10.2",
    "torch==2.13.0",
    "transformers>=5.17.0,<5.18.0",
    "unicategories==0.1.2",
    "unicodedataplus==16.0.0.post1",
    "urllib3==2.7.0",
    "uvicorn[standard]==0.40.0",
]

[project.optional-dependencies]
benchmark = [
    "matplotlib==3.11.0",
    "psutil==7.2.2",
    "scipy>=1.13.0,<1.19.0",
]

dev = [
    "black==26.3.1",
    "detect-secrets==1.5.0",
    "faker==40.8.0",
    "flake8==7.3.0",
    "httpx==0.28.1",  # For testing FastAPI
    "interrogate==1.7.0",
    "mypy==2.1.0",
    "pre-commit==4.5.1",
    "pytest==9.0.3",
    "pytest-asyncio==1.4.0",
    "pytest-cov==7.0.0",
    "pytest-mock==3.15.1",
    "pytest-timeout==2.3.1",
    "pytest-xdist==3.6.1",
    "ruff==0.15.20",
]

notebooks = [
    "ipykernel==7.3.0",
    "jupyter==1.1.1",
    "matplotlib==3.11.0",
    "nbconvert==7.17.1",
    "nbformat==5.10.4",
    "seaborn==0.13.2",
]

telemetry = [
    "opentelemetry-api==1.41.1",
    "opentelemetry-exporter-otlp-proto-grpc==1.41.1",
    "opentelemetry-instrumentation-fastapi==0.62b1",
    "opentelemetry-sdk==1.41.1",
]

rapidocr = []

easyocr = [
    "docling[easyocr]==2.127.0",
]

ocrmac = [
    "docling[ocrmac]==2.127.0",
]

tesserocr = [
    "docling[tesserocr]==2.127.0",
]

vlm_asr = [
    "docling[vlm,asr]==2.127.0",
]

[project.urls]
Homepage = "https://github.com/IBM/docling-pipelines"
Documentation = "https://github.com/IBM/docling-pipelines/blob/main/docs/README.md"
Source = "https://github.com/IBM/docling-pipelines"
"Bug Tracker" = "https://github.com/IBM/docling-pipelines/issues"

[project.scripts]
docling-pipelines-api = "docpipe.api.main:run"
docling-pipelines = "docpipe.cli.docpipe_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/docpipe"]


[tool.hatch.build]
exclude = [
    ".venv/**",
    ".vtest/**",
    "**/__pycache__/**",
    "**/*.pyc",
]

[tool.hatch.build.targets.sdist]
exclude = [
    ".venv/",
    ".vtest/",
    "*.pyc",
    "__pycache__/"
]

[tool.hatch.version]
source = "vcs"

[tool.uv]
override-dependencies = [
    # CVE-2026-59939: Security fix for httplib2 vulnerability (transitive via google-auth-httplib2).
    "httplib2>=0.32.0",
    # Dependabot: h2 <= 4.4.0 — duplicate Host header request smuggling (transitive via httpx).
    "h2==4.4.1",
    # Dependabot: jupyterlab >= 4.6.0, <= 4.6.1 — XSS and enforcement bypass (transitive via jupyter).
    "jupyterlab==4.6.2",
    # Pin rapidocr to 3.9.0 within docling-slim's >=3.8,<4.0 constraint.
    "rapidocr==3.9.0",
    # CVE-2026-49476, CVE-2026-49477: Security fix for soupsieve vulnerability (transitive via beautifulsoup4).
    "soupsieve>=2.8.4",
]

[tool.ruff]
# Set the maximum line length to 120 characters
line-length = 120
# Target Python 3.12
target-version = "py312"

[tool.ruff.lint]
# Enable pycodestyle (E, W), Pyflakes (F), isort (I), and other useful rules
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # Pyflakes
    "I",     # isort
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "C4",    # flake8-comprehensions
    "SIM",   # flake8-simplify
    "RUF",   # Ruff-specific rules
    "RET",   # flake8-return
    "PTH",   # flake8-use-pathlib (issue #6399)
    "DTZ",   # flake8-datetimez — require timezone-aware datetimes (issue #6401)
    "T10",   # flake8-debugger
    "EM",    # flake8-errmsg
    "ISC",   # flake8-implicit-str-concat
    "ICN",   # flake8-import-conventions
    "PIE",   # flake8-pie
    "PT",    # flake8-pytest-style
    "Q",     # flake8-quotes
    "RSE",   # flake8-raise
]
# Ignore specific rules if needed
ignore = [
    "UP035",  # Import from `collections.abc` instead of `typing` (keep typing imports)
    "E501",   # Line too long (let formatter handle line length)
    "SIM",    # flake8-simplify - ignore all simplification suggestions (nested structures, etc.)
    "N818",   # Exception name should end with Error suffix
    "B027",   # Empty method in abstract base class without abstract decorator (intentional for optional overrides)
    "EM",     # flake8-errmsg
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["PT003", "PT006", "PT009", "PT011", "PT012", "PT013", "PT018", "PT027", "PT028"]
"tests/*.py" = ["PT003", "PT006", "PT009", "PT011", "PT012", "PT013", "PT018", "PT027", "PT028"]

[tool.ruff.lint.isort]
# Configure import sorting
# NOTE: Ruff's isort implementation is the single source of truth for import sorting.
# No conflicting isort configurations exist in .isort.cfg or setup.cfg files.
known-first-party = ["docpipe"]

[tool.ruff.format]
# Quote style standardization: Converting all string quotes to double quotes for consistency
# This will automatically convert single quotes to double quotes throughout the codebase
# Note: Raw strings (r"..."), f-strings (f"..."), and docstrings are handled correctly
quote-style = "double"
# Indent with spaces
indent-style = "space"
# Respect magic trailing commas
skip-magic-trailing-comma = false

[tool.mypy]
mypy_path = "."
namespace_packages = true
explicit_package_bases = true
check_untyped_defs = true
plugins = ["pydantic.mypy"]
# Treat the backend directory as the source root
files = ["."]
# Set Python version to 3.12 to support PEP 695 type parameter syntax
python_version = "3.12"
# Disable import-untyped errors for third-party libraries without type stubs
disable_error_code = ["import-untyped"]

[[tool.mypy.overrides]]
module = [
    "pyarrow",
    "pyarrow.*",
    "data_processing",
    "data_processing.*",
    "nltk",
    "nltk.*",
    "fasttext",
    "fasttext.*",
    "langdetect",
    "langdetect.*",
    "dpk_enrichment",
    "dpk_enrichment.*",
    "dpk_doc_quality",
    "dpk_doc_quality.*",
    "dpk_readability",
    "dpk_readability.*",
    "dpk_ededup",
    "dpk_ededup.*",
    "dpk_doc_id",
    "dpk_doc_id.*",
    "msal",
    "msal.*",
    "psutil",
    "psutil.*",
    "cachetools",
    "cachetools.*",
    "google_auth_oauthlib",
    "google_auth_oauthlib.*",
    "babel",
    "babel.*",
    "babel.core",
    "babel.numbers",
    "datefinder",
    "datefinder.*",
    "re2",
    "re2.*",
    "opentelemetry",
    "opentelemetry.*"
]
ignore_missing_imports = true
follow_imports = "silent"

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.interrogate]
fail-under = 90
ignore-init-method = true
ignore-magic = true
ignore-private = true
exclude = ["tests", "scripts"]
verbose = 0

[tool.bandit]
exclude_dirs = ["tests", "scripts"]
skips = ["B101"]  # Allow assert statements in tests

[tool.coverage.run]
source = ["src/docpipe"]
omit = [
    "tests/*",
    "**/__pycache__/*",
    "src/docpipe/integrations/*",  # External integrations — covered by integration tests
]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if TYPE_CHECKING:",
    "@(abc\\.)?abstractmethod",
    "raise NotImplementedError",
]

[dependency-groups]
dev = [
    "anyio==4.14.2",
    "httpx==0.28.1",
    "mypy==2.1.0",
    "pytest==9.0.3",
    "pytest-asyncio==1.4.0",
    "types-boto3==1.43.0",
    "types-python-dateutil==2.9.0.20260408",
    "types-requests==2.33.0.20260408",
]
